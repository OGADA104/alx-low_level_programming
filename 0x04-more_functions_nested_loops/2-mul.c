#include "main.h"
#include<stdio.h>
/**
*mul - multiplies inputs
*@a: first input int
*@b: second input int
*Return: returns the results after multiplication
*/
int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
