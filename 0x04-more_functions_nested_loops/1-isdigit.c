#include "main.h"
#include<ctype.h>
/**
*_isdigit -checks if input is a digit 0-9
*@c: input to check
*Return: 1 if digit otherwise 0
*/
int _isdigit(int c)
{
	if (isdigit(c))
		return (1);
	else
		return (0);
	return (0);
}
