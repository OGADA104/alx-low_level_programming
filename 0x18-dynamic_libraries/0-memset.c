#include "main.h"

/**
**_memset - fills the first n bytes to s by b
*@s: pointer
*@b: bytes to fill
*@n: bytes memory area
*Return: char
*/
char *_memset(char *s, char b, unsigned int n)
{
	return (0);
}
