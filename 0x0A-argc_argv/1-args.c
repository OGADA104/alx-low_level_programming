#include<stdlib.h>
#include<stdio.h>

/**
*main - print no of args
*@argc: arguments count
*@argv: arguments passed
*Return: 0 if success
*
*
*/
int main(int argc, char *argv[])
{
	if (argv[] != NULL)
	{
		printf("%d", argc);
	}
	printf('\n');
	return (0);
}
